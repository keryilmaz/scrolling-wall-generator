# Core dependencies
gradio>=5.33.0
numpy>=1.24.0
Pillow>=9.0.0
moviepy>=2.0.0

# Video processing
imageio>=2.9.0
imageio-ffmpeg>=0.4.0

# Audio processing (for music sync feature)
librosa>=0.10.0
soundfile>=0.12.0  # Required by librosa

# Optional but recommended
tqdm>=4.65.0  # Progress bars for MoviePy